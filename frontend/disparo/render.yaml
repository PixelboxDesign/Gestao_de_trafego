services:
  - type: web
    name: luna-disparo
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LUNA_API_URL
        value: https://repackage-backstage-snowcap.ngrok-free.dev
