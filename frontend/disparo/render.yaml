services:
  - type: web
    name: luna-disparo
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LUNA_API_URL
        value: https://saying-gardening-optimize-rotation.trycloudflare.com
